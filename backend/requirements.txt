# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# Config / validation
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Realtime
websockets>=12.0

# Persistence
SQLAlchemy>=2.0

# NOTE: service-specific deps (iFlytek SDK, DeepSeek/OpenAI client, silero-vad)
# are added in the PRs that introduce those features.
