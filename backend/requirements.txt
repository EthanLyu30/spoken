# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# Config / validation
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Realtime
websockets>=12.0

# Persistence (SQLite by default; psycopg enables Postgres via DATABASE_URL)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# HTTP client (DeepSeek chat-completions API)
httpx>=0.27

# NOTE: remaining service-specific deps (iFlytek SDK, silero-vad) are added in
# the PRs that introduce those features.
